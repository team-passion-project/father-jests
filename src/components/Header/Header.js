import React from 'react'
import Navbar from 'react-bootstrap/Navbar'

const Header = () =>
  <Navbar fixed="top" className="navbar-header">
    <Navbar.Brand>
      <div className="logo">
        FATHER JESTS
      </div>
    </Navbar.Brand>
  </Navbar>

export default Header